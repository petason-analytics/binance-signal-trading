import { Test, TestingModule } from '@nestjs/testing';
import { BoxCampaignResolver } from './box-campaign.resolver';

describe('BoxCampaignResolver', () => {
  let resolver: BoxCampaignResolver;

  beforeEach(async () => {
    // const module: TestingModule = await Test.createTestingModule({
    //   providers: [BoxCampaignResolver],
    // }).compile();
    // resolver = module.get<BoxCampaignResolver>(BoxCampaignResolver);
  });

  it('should be defined', () => {
    // expect(resolver).toBeDefined();
  });
});
